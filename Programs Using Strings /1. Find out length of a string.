#include <stdio.h>

int main() {
    char str[100] = "Hello, RAJ!";
    int length = 0;

    // Count characters until null terminator
    while (str[length] != '\0') {
        length++;
    }

    printf("Length of the string: %d\n", length);

    return 0;
}
