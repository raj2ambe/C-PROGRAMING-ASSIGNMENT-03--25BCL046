#include <stdio.h>

int main() {
    char str[100] = "Hello RAJ!";
    int i = 0;

    // Print each character on a new line
    while (str[i] != '\0') {
        printf("%c\n", str[i]);
        i++;
    }

    return 0;
}
